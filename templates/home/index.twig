{% extends "layout.twig" %}

{% block title %}Accueil - SOD Tirage au Sort{% endblock %}

{% block content %}
    <h1>Bienvenue sur l'application de tirage au sort SOD</h1>
    <p>Cette application gère le rituel du Speaker of the Day pour l'école Holberton Thonon-les-Bains.</p>
    
    {% if user %}
        <p>Vous êtes connecté en tant que {{ user.username }}.</p>
        <a href="{{ url_for('auth.logout') }}">Se déconnecter</a>
    {% else %}
        <p>Veuillez vous connecter pour accéder à toutes les fonctionnalités.</p>
        <a href="{{ url_for('auth.loginForm') }}">Se connecter</a>
    {% endif %}
{% endblock %}
